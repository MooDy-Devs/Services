name: Deploy and Handle Orders

on:
  push:
    branches:
      - main  # Trigger on push to the main branch
  workflow_dispatch:  # Allow manual triggering

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Deploy to Vercel (or your preferred platform)
        run: |
          # Add your deployment commands here
          echo "Deploying..."

  handle-order:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Send order to Discord
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          # Example: Send a test message to Discord
          curl -X POST \
            -H "Content-Type: application/json" \
            -d '{
              "username": "Order Bot",
              "avatar_url": "https://i.imgur.com/a/A3zGHbh",
              "embeds": [{
                "title": "📬 Test Order Received",
                "color": 0x8A2BE2,
                "fields": [
                  { "name": "Service", "value": "Test Service", "inline": true },
                  { "name": "Telegram Username", "value": "TestUser", "inline": true },
                  { "name": "Project Details", "value": "This is a test order." }
                ],
                "timestamp": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'",
                "footer": {
                  "text": "Mohamed Pro Services Order System",
                  "icon_url": "https://i.imgur.com/a/A3zGHbh"
                }
              }]
            }' \
            "$DISCORD_WEBHOOK_URL"
